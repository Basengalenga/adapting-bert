# Install PyTorch from the CUDA wheel index FIRST, then this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt

torch>=2.2.0
transformers>=4.46.0     # Trainer(processing_class=...) and eval_strategy
datasets>=2.18.0
accelerate>=0.30.0

# Entity-level metrics (Precision / Recall / F1)
seqeval>=1.2.2

# Model 1: classical ML classifier
scikit-learn>=1.3.0

# Hub publishing
huggingface_hub>=0.23.0

# Notebook
numpy>=1.24.0
pandas>=2.0.0
jupyter
